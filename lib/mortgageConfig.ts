/**
 * Mortgage calculator — tunable defaults (adjust when refreshing product copy).
 */
/** Default annual mortgage interest rate (%) on first load */
export const DEFAULT_ANNUAL_MORTGAGE_RATE = 3.25;
